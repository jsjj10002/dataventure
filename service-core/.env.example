# Backend Core 환경 변수 예시
# .env.example 파일을 .env로 복사하여 실제 값으로 채워주세요

# ===== 서버 설정 =====
PORT=8080
NODE_ENV=development

# ===== 데이터베이스 (PostgreSQL) =====
# 발급 방법: 로컬 PostgreSQL 설치 또는 GCP Cloud SQL 인스턴스 생성
# 형식: postgresql://[사용자]:[암호]@[호스트]:[포트]/[DB명]?schema=public
DATABASE_URL="postgresql://postgres:yourpassword@localhost:5432/flex_recruiter?schema=public"

# ===== JWT 인증 =====
# JWT 시크릿 키 (최소 32자 이상의 랜덤 문자열 권장)
# 생성 방법: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars
JWT_EXPIRES_IN=7d

# ===== AI 서비스 URL =====
# service-ai (FastAPI) 주소
AI_SERVICE_URL=http://localhost:8000

# ===== 파일 업로드 (GCP Cloud Storage) =====
# 발급 방법: GCP 콘솔 > Cloud Storage > 버킷 생성
# 이력서 및 음성 파일 저장용
GCP_PROJECT_ID=your-gcp-project-id
GCP_STORAGE_BUCKET=flex-recruiter-files

# ===== GCP 인증 (선택) =====
# 발급 방법: GCP 콘솔 > IAM > 서비스 계정 > 키 생성 (JSON)
# GOOGLE_APPLICATION_CREDENTIALS=./gcp-service-account-key.json
